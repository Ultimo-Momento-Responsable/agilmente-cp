import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'agilmente-cp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
