import { ResultTranslatePipe } from './result-translate.pipe';

describe('ResultTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new ResultTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
